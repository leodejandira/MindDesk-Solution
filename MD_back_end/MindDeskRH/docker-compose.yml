version: '3.8'

services:
  api:
    build: .
    container_name: minha-api
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: always
    extra_hosts:
      - "host.docker.internal:host-gateway"